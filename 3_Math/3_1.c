// write a program that read any integer number and display absolute value.

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;

    scanf("%d", &n);

    printf("Absolute: %d\n", abs(n));

    return 0;
}